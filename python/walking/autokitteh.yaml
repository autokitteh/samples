version: v1

project:
  name: walking
  connections:
    - name: http_trigger
      integration: http
      token: event
    - name: google
      integration: google
  triggers:
    - name: events
      connection: http_trigger
      event_type: post
      entrypoint: walking.py:on_event
